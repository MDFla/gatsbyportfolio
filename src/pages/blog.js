import React from "react"
import { Link } from 'gatsby'
import Layout from "../components/layout";

const BlogPage = () => {
    return (
        <div>
            <Layout>
            <h1>Blog</h1>
            <p>This is where the blog will show!</p>
            </Layout>
        </div>
    )
}

export default BlogPage